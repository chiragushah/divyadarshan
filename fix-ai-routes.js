const fs=require('fs'),path=require('path'),files={"app/api/ai/planner/route.ts": "import { NextRequest, NextResponse } from 'next/server'\nimport { getServerSession } from 'next-auth'\nimport { authOptions } from '@/lib/auth/options'\nimport { routeAI, SYSTEM_PROMPTS } from '@/lib/ai/router'\nimport connectDB from '@/lib/mongodb/connect'\nimport { AIUsageLog } from '@/models'\n\nexport const dynamic = 'force-dynamic'\n\nexport async function POST(req: NextRequest) {\n  const start = Date.now()\n  const session = await getServerSession(authOptions)\n  const body = await req.json()\n  const { destination, days, pilgrims, budget, transport, special_requests } = body\n\n  const userMessage = `Plan a ${days}-day pilgrimage to ${destination} for ${pilgrims || 1} pilgrim(s).\nBudget: ${budget || 'moderate'} per person.\nTransport: ${transport || 'mixed'}.\n${special_requests ? `Special requests: ${special_requests}` : ''}\nPlease provide a detailed day-by-day itinerary.`\n\n  try {\n    const result = await routeAI('planner', SYSTEM_PROMPTS.planner, userMessage, { maxTokens: 3000 })\n    const duration_ms = Date.now() - start\n\n    await connectDB()\n    await AIUsageLog.create({\n      user_id:     session?.user?.id || 'anonymous',\n      user_email:  session?.user?.email || '',\n      feature:     'planner',\n      provider:    result.provider,\n      duration_ms,\n      destination: destination || '',\n      success:     true,\n    }).catch(() => {})\n\n    return NextResponse.json({ itinerary: result.content, provider: result.provider })\n  } catch (err: any) {\n    await connectDB()\n    await AIUsageLog.create({\n      user_id:     session?.user?.id || 'anonymous',\n      user_email:  session?.user?.email || '',\n      feature:     'planner',\n      provider:    'claude',\n      duration_ms: Date.now() - start,\n      success:     false,\n      error_msg:   err.message,\n    }).catch(() => {})\n    return NextResponse.json({ error: err.message }, { status: 500 })\n  }\n}\n", "app/api/ai/search/route.ts": "import { NextRequest, NextResponse } from 'next/server'\nimport { getToken } from 'next-auth/jwt'\nimport { routeAI, SYSTEM_PROMPTS } from '@/lib/ai/router'\nimport connectDB from '@/lib/mongodb/connect'\nimport { SearchLog, AIUsageLog } from '@/models'\nimport { Temple } from '@/models'\n\nexport const dynamic = 'force-dynamic'\n\nexport async function POST(req: NextRequest) {\n  const start = Date.now()\n  const { query } = await req.json()\n  const token = await getToken({ req, secret: process.env.NEXTAUTH_SECRET })\n\n  try {\n    // First try direct DB search\n    await connectDB()\n    const temples = await Temple.find({\n      $or: [\n        { name:  { $regex: query, $options: 'i' } },\n        { city:  { $regex: query, $options: 'i' } },\n        { state: { $regex: query, $options: 'i' } },\n        { deity: { $regex: query, $options: 'i' } },\n        { type:  { $regex: query, $options: 'i' } },\n      ]\n    }).limit(8).select('name city state deity type slug has_live').lean()\n\n    const duration_ms = Date.now() - start\n\n    // Log analytics\n    await Promise.all([\n      SearchLog.create({ user_id: token?.sub || 'anonymous', user_email: (token?.email as string) || '', query, provider: 'mongodb', duration_ms }).catch(() => {}),\n      AIUsageLog.create({ user_id: token?.sub || 'anonymous', user_email: (token?.email as string) || '', feature: 'search', provider: 'groq', duration_ms, success: true }).catch(() => {}),\n    ])\n\n    if (temples.length > 0) {\n      return NextResponse.json({ results: temples, provider: 'mongodb' })\n    }\n\n    // If no DB results, use Groq AI\n    const result = await routeAI('search', SYSTEM_PROMPTS.search, query)\n    return NextResponse.json({ results: [], aiSuggestion: result.content, provider: result.provider })\n  } catch (err: any) {\n    return NextResponse.json({ error: err.message }, { status: 500 })\n  }\n}\n", "app/api/ai/checklist/route.ts": "import { NextRequest, NextResponse } from 'next/server'\nimport { getServerSession } from 'next-auth'\nimport { authOptions } from '@/lib/auth/options'\nimport { routeAI, SYSTEM_PROMPTS } from '@/lib/ai/router'\nimport connectDB from '@/lib/mongodb/connect'\nimport { AIUsageLog } from '@/models'\n\nexport const dynamic = 'force-dynamic'\n\nexport async function POST(req: NextRequest) {\n  const start = Date.now()\n  const session = await getServerSession(authOptions)\n  const body = await req.json()\n  const { destination, days, mode, season, pilgrim_type, special_needs } = body\n\n  const userMessage = `Generate a packing checklist for:\nDestination: ${destination}\nDuration: ${days} days\nTravel mode: ${mode || 'mixed'}\nSeason: ${season || 'Winter (Oct-Mar)'}\nPilgrim type: ${pilgrim_type || 'Couple'}\n${special_needs ? `Special needs: ${special_needs}` : ''}\n\nReturn as JSON with this exact structure:\n{\n  \"categories\": [\n    {\n      \"name\": \"Category Name\",\n      \"items\": [\n        { \"name\": \"Item name\", \"priority\": \"must\" | \"recommended\", \"note\": \"optional tip\" }\n      ]\n    }\n  ],\n  \"destination_tips\": [\"tip1\", \"tip2\", \"tip3\"]\n}`\n\n  try {\n    const result = await routeAI('checklist', SYSTEM_PROMPTS.checklist, userMessage, { maxTokens: 2000 })\n    const duration_ms = Date.now() - start\n\n    // Parse JSON response\n    let checklist\n    try {\n      const clean = result.content.replace(/```json|```/g, '').trim()\n      checklist = JSON.parse(clean)\n    } catch {\n      checklist = { categories: [], destination_tips: [result.content] }\n    }\n\n    await connectDB()\n    await AIUsageLog.create({\n      user_id:     session?.user?.id || 'anonymous',\n      user_email:  session?.user?.email || '',\n      feature:     'checklist',\n      provider:    result.provider,\n      duration_ms,\n      destination: destination || '',\n      success:     true,\n    }).catch(() => {})\n\n    return NextResponse.json({ checklist, provider: result.provider })\n  } catch (err: any) {\n    return NextResponse.json({ error: err.message }, { status: 500 })\n  }\n}\n"};
for(const[fp,c]of Object.entries(files)){fs.mkdirSync(path.dirname(fp),{recursive:true});fs.writeFileSync(fp,c,'utf8');console.log('✅ '+fp);}
console.log('Done! Now run: git add . && git commit -m "Fix AI routes" && git push');